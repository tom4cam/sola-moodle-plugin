{
  "name": "sola-xai-rt-proxy",
  "version": "1.0.0",
  "description": "WebSocket proxy for xAI Realtime so the xAI master API key never reaches the browser. Accepts learner connections carrying a short-lived signed token from Moodle and relays frames to api.x.ai.",
  "main": "server.js",
  "private": true,
  "license": "GPL-3.0-or-later",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "LOG_LEVEL=debug node server.js"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
